// import {} from './actions'

const initialState = {
    inProgress: false,
    severError: {},
    solarData: {},
}

export const solarDataReducer = (state = initialState, { type, payload}) => {
    switch (type) {
        default: 
            return state
    }
}